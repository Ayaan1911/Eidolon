# Notes

Future additions considered but not done yet, and why:

- **CI/tests badge** — no `.github/workflows/` exists yet, so there's no real CI run for a badge
  to report on. Add a GitHub Actions workflow (even a minimal lint/test run) first, then the badge.
